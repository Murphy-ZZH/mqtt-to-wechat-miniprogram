<!-- index.wxml -->
<view class="container">
  <map class="map" latitude="{{result[1]}}" longitude="{{result[0]}}" scale="{{scale}}" markers="{{markers}}" show-location></map>
  <view class="info">
    <view>经度: {{result[0]}}</view>
    <view>纬度: {{result[1]}}</view>
  </view>
</view>
